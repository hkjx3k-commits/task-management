# モック改善 実装TODO

作成日：2026年5月8日  
対象ファイル：`mock/index.html`

---

## 進捗状況

| # | フェーズ | 内容 | 状態 |
|---|---|---|---|
| 1 | フェーズ1 | データモデル拡張（priority / assignee / subtasks） | ✅ 完了 |
| 2 | フェーズ2 | 軽量UI改善（B-2 カード数バッジ / B-4 期日相対表示） | ✅ 完了 |
| 3 | フェーズ3 | カード機能追加（A-2 優先度 / A-3 担当者 / A-4 チェックリスト） | ✅ 完了 |
| 4 | フェーズ4 | モーダル2カラムレイアウト（B-3） | ✅ 完了 |
| 5 | フェーズ5 | ドラッグ&ドロップ（A-1 ネイティブHTML5） | ✅ 完了（リスト内並び替え＋リスト間移動） |
| 6 | フェーズ6 | 検索・フィルター（A-5） | ✅ 完了 |
| 7 | フェーズ7 | ヘッダーナビゲーション（B-1） | ✅ 完了 |
| 8 | フェーズ8 | ダッシュボード画面（C-2） | ✅ 完了 |
| 9 | フェーズ9 | ダークモード対応（B-5） | ✅ 完了 |
| 10 | ドキュメント | 実装TODOの進捗更新 | ✅ 完了 |

---

## 各フェーズの詳細

### フェーズ1：データモデル拡張 ✅
`getInitialState()` のカードデータに以下フィールドを追加済み：
```javascript
priority: "high" | "medium" | "low"   // A-2 優先度（デフォルト: "medium"）
assignee: ""                           // A-3 担当者名
subtasks: []  // { id, title, done }   // A-4 チェックリスト
```
古い localStorage データへの後方互換マイグレーションも実装済み（`loadState()` でスプレッド演算子によるデフォルト値補完）。

### フェーズ2：軽量UI改善 ✅
- **B-2**：`createListEl()` のリストヘッダーにカード数バッジを追加。検索・フィルター中は「絞り込み件数/全件数」を表示。
- **B-4**：`formatDue(dateStr)` 関数を追加（「今日」「明日」「3日後」「昨日」「2日前」などに変換）。

### フェーズ3：カード機能追加 ✅
- **A-2**：優先度バッジをカードに表示（高=赤/中=オレンジ/低=緑）、モーダルに `<select>` 追加。
- **A-3**：モーダルに担当者 `<input>` 追加、カードに担当者名を小さく表示（👤アイコン付き）。
- **A-4**：モーダルにチェックリストUI追加（追加・チェック・削除）、カードに「2/5 完了」とプログレスバーを表示。

### フェーズ4：モーダルレイアウト刷新 ✅
- モーダル幅 `480px` → `720px`
- 左カラム：タスク名・メモ・チェックリスト
- 右カラム：期日・優先度・担当者・保存/削除ボタン

### フェーズ5：ドラッグ&ドロップ ✅
- ネイティブ HTML5 Drag and Drop API を使用
- リスト内並び替え＋リスト間カード移動の両方に対応
- `dragover` の `preventDefault()` が必須（HTML5 D&Dの仕様）
- `position` フィールドを振り直す方式で、D&D後のソートボタン押下も正常動作

### フェーズ6：検索・フィルター ✅
- ヘッダーに検索バーを設置（タイトル・担当者・メモに対してリアルタイムフィルタリング）
- 「すべて」「期日切れ」「今日」の3種類のフィルターボタン
- 検索中はカード数バッジが「絞り込み件数/全件数」表示に切り替わる

### フェーズ7：ヘッダーナビゲーション ✅
- 「ボード」「ダッシュボード」タブを追加
- `showPage(name)` 関数でページ切り替え
- URLハッシュ（`#board` / `#dashboard`）でブラウザバック対応

### フェーズ8：ダッシュボード画面 ✅
- サマリーカード（タスク総数・期日切れ数・今日期限数・完了数）
- 期日切れカード一覧・今日期限カード一覧
- 優先度別タスク数グラフ（CSSバーグラフ、ライブラリ不要）
- 「完了」という文字を含むリストのカードを完了済みとみなす

### フェーズ9：ダークモード対応 ✅
- CSS変数（`--bg`、`--surface`、`--card-bg`、`--text` など）でカラーを集約
- ヘッダー右端に🌙/☀️ トグルボタンを追加
- テーマ設定を `localStorage`（`taskboard_dark`キー）に保存・復元

### ドキュメント更新 ✅
- `docs/実装TODO.md`：全フェーズの進捗を更新
