# モック改善 実装TODO

作成日：2026年5月8日  
対象ファイル：`mock/index.html`

---

## 進捗状況

| # | フェーズ | 内容 | 状態 |
|---|---|---|---|
| 1 | フェーズ1 | データモデル拡張（priority / assignee / subtasks） | ⬜ 未着手 |
| 2 | フェーズ2 | 軽量UI改善（B-2 カード数バッジ / B-4 期日相対表示） | ⬜ 未着手 |
| 3 | フェーズ3 | カード機能追加（A-2 優先度 / A-3 担当者 / A-4 チェックリスト） | ⬜ 未着手 |
| 4 | フェーズ4 | モーダル2カラムレイアウト（B-3） | ⬜ 未着手 |
| 5 | フェーズ5 | ドラッグ&ドロップ（A-1 ネイティブHTML5） | ⬜ 未着手 |
| 6 | フェーズ6 | 検索・フィルター（A-5） | ⬜ 未着手 |
| 7 | フェーズ7 | ヘッダーナビゲーション（B-1） | ⬜ 未着手 |
| 8 | フェーズ8 | ダッシュボード画面（C-2） | ⬜ 未着手 |
| 9 | フェーズ9 | ダークモード対応（B-5） | ⬜ 未着手 |
| 10 | ドキュメント | データ設計書・機能一覧・画面設計書・画面遷移図の更新 | ⬜ 未着手 |

---

## 各フェーズの詳細

### フェーズ1：データモデル拡張
`getInitialState()` のカードデータに以下フィールドを追加：
```javascript
priority: "high" | "medium" | "low" | ""  // A-2 優先度
assignee: ""                               // A-3 担当者
subtasks: []  // { id, title, done }       // A-4 チェックリスト
```

### フェーズ2：軽量UI改善
- **B-2**：`createListEl()` のリストヘッダーにカード数バッジを追加
- **B-4**：`formatDue(dateStr)` 関数を追加（「今日」「3日後」「昨日」などに変換）

### フェーズ3：カード機能追加
- **A-2**：優先度バッジをカードに表示（高=赤/中=オレンジ/低=緑）、モーダルに `<select>` 追加
- **A-3**：モーダルに担当者 `<input>` 追加、カードに担当者名を小さく表示
- **A-4**：モーダルにチェックリストUI追加（追加・チェック・削除）、カードに「2/5 完了」を表示

### フェーズ4：モーダルレイアウト刷新
- モーダル幅 `480px` → `720px`
- 左カラム：タスク名・メモ・チェックリスト
- 右カラム：期日・優先度・担当者・操作ボタン

### フェーズ5：ドラッグ&ドロップ
- ネイティブ HTML5 Drag and Drop API を使用
- `draggable="true"` 属性を付与
- `dragstart` / `dragover` / `drop` イベントで位置を再計算
- リスト間移動 + 同一リスト内並び替えの両方に対応

### フェーズ6：検索・フィルター
- ヘッダー右側に検索バーを設置
- キーワードでリアルタイムフィルタリング
- 優先度・期日状態（期日切れ/今日/すべて）で絞り込みボタン

### フェーズ7：ヘッダーナビゲーション
- 「ボード」「ダッシュボード」タブを追加
- `showPage(name)` 関数でページ切り替え
- URLハッシュ（`#board` / `#dashboard`）でブラウザバック対応

### フェーズ8：ダッシュボード画面
- サマリーカード（タスク総数・期日切れ数・今日期限数・完了数）
- 期日切れカード一覧・今日期限カード一覧
- 優先度別タスク数グラフ（CSSバーグラフ、ライブラリ不要）

### フェーズ9：ダークモード対応
- CSS変数（`--bg`, `--surface`, `--card-bg`, `--text` など）でカラーを集約
- ヘッダー右端に🌙/☀️ トグルボタンを追加
- テーマ設定を `localStorage` に保存

### ドキュメント更新
- `docs/データ設計書.md`：CARDエンティティに `priority`・`assignee`・`subtasks` を追記
- `docs/機能一覧.md`：A-1〜A-5、B-1〜B-5、C-2 の機能・受け入れ条件を追記
- `docs/画面設計書.md`：ダッシュボード画面レイアウト・モーダル2カラム仕様を追加
- `docs/画面遷移図.md`：ボード ↔ ダッシュボード の遷移を追加
